/*! product-ordering 2017-11-07 */

const storedProducts=JSON.parse(localStorage.getItem("betsyDatabase")).products,productsEl=document.getElementById("products_section");let productsContent="";const updateProducts=storedProducts.forEach(t=>{productsContent+=`\n        <article id="product_${t.productId}" class="product">\n            <section id="info_${t.productId}" class="info">\n                <h1>${t.title}</h1>\n                <img src="${t.image}" alt="${t.title}">\n                <p>${t.description}</p>\n                <p>Price: ${t.price}</p>\n                <p>Number Available: ${t.quantity}</p>\n            </section>\n            <section id="review_${t.productId}" class="review">\n                <h1>Reviews of ${t.title}</h1>\n            </section>\n        </article>\n    `});productsEl.innerHTML+=productsContent,module.exports=updateProducts;